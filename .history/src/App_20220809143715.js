import { useState } from 'react';
import './App.css';

const  App = ()=> {
  const student= [{name : "kiran" ,age:"10" },
  {name:"indra", age:"10"},
  {name:"ram" ,age:"10"}];

  function newstudent(Student) { return names.map((newName, newAge, class { })); }=>{ 
  let classtenStudent= newName.name; newAge.age
  return classtenStudent

})
}
  
  return (
    <div className="App">
      <div>
     Test the data
      </div>
    </div>
  );
}

export default App;
