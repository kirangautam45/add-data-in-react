import { useState } from 'react';
import './App.css';

const  App = ()=> {
  const student= [{name : "kiran" ,age:"10" },
  {name:"indra", age:"10"},
  {name:"ram" ,age:"10"}];

  f newstudent(student) { return student.map((newName, newAge, class { })); }=>{ 
  let classTenStudent= newName.name; newAge.age
  return classTenStudent

})
}
  
  return (
    <div className="App">
      <div>
     Test the data
      </div>
    </div>
  );
}

export default App;
