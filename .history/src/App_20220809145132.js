import { useState } from 'react';
import './App.css';

const  App = ()=> {
 

  let newstudent => (student) { return student.map((newName, newAge, class { })); }=>{ 
  let classTenStudent= newName.name; newAge.age
  return classTenStudent

}

  
  return (
    <div className="App">
      <div>
     Test the data
      </div>
    </div>
  );
}

export default App;
