import { useState } from 'react';
import './App.css';

const  App = ()=> {
  const names= [{name : "kiran" ,age:"10" },
  {name:"indra", age:"10"},
  {name:"ram" ,age:"10"}];

const newstudent = (names)=> {return names.map((newName, newAge)=>{
  let classtenStudent= newName.name, newAge:age, "class"

})
}
  
  return (
    <div className="App">
      <div>
     Test the data
      </div>
    </div>
  );
}

export default App;
